export function helixSnippet(): string {
  return `const el = document.querySelector('#helix-button'); if (el) { el.click(); } const closeBtn = document.querySelector('.vex-close.aptr-engagement-close-btn.px-close-button'); if (closeBtn) { closeBtn.click(); }`;
}
